# Public Supabase project URL. Never use service_role or sb_secret_ keys.
VITE_SUPABASE_URL=
# Set ONE public key. PUBLISHABLE_KEY takes precedence if both are non-empty.
VITE_SUPABASE_ANON_KEY=
VITE_SUPABASE_PUBLISHABLE_KEY=
